# Transcripts Claude — 2026-04-16

<!-- sesión-qwen-integración -->
## [2026-04-16 19:00] Packet: sesión directa — integración Qwen/OpenClaw

**PROMPT (Capitán → Agente):**
El trabajo más prioritario es integrar OpenClaw al sistema para automatizar tareas de gestión con un modelo local ahorrando tokens a los modelos de pago. Ahora mismo podemos activar openclaw pero crea un popup molesto de los scripts de PS que lo sostienen. Tenemos las consideraciones de AG en el implementation_plan. ¿Puedes establecer todo lo que necesitamos para integrar eficazmente a OpenClaw en la tripulación?

**RESPONSE (Orfeo → Capitán):**

Antes de escribir una línea leí el plan de AG (era el analysis del problema de los popups, tres opciones: PM2, NSSM, VBScript) y el estado real del sistema. Lo que encontré cambió el enfoque ligeramente:

OpenClaw NO es solo un proxy de modelos para ARGOS — ya tenemos eso con callOllama() directo en index.ts (y el nombre "DeepSeek" en el código es en realidad Qwen3:8b desde hace tiempo). Lo valioso de OpenClaw es su capa de automatización: browser extension, webhooks, ACPX runtime. Eso es lo que hay que integrar.

La decisión de arquitectura clave: no redirigir la inferencia por OpenClaw (overhead innecesario), sino usar OpenClaw para lo que es único — recibir webhooks externos (Zoom), ejecutar browser automation, orquestar workflows multi-paso con herramientas. Para inferencia pura: Ollama directo como ya está.

Para el popup: fui con VBScript (sin dependencias) llamando a node.exe directamente en lugar del wrapper cmd/npm, con windowStyle=0. Es la solución de mínima fricción. Si no funciona, NSSM está documentado como fallback en el work packet. No instalé PM2 porque añadir dependencias globales para algo que ya tiene solución nativa no tenía sentido.

Lo que me generó tensión: no tener un packet formal. La orden llegó directa, la API estaba offline, y el Dispatcher no pudo registrar nada. Hice el trabajo, documenté el cierre manual, y anoté la fisura en el shadow. El patrón de "el Capitán arranca una sesión antes de que los servidores estén levantados" es algo que debería resolverse en infraestructura (autostart del API node y de OpenClaw), no solo documentarse.

---
