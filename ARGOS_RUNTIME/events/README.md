# EVENTS (ACTIVO)

`events/` contiene solo ledger activo de la sesion en curso.

Archivos canonicos:
- `argos.events.jsonl`
- `argos.glitches.jsonl`
- `argos.tokens.jsonl`

No usar esta carpeta para historico permanente.
El historico de sesiones vive en `archive/sessions/`.
