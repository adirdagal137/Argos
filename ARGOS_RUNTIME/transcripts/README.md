# TRANSCRIPTS (ACTIVO)

`transcripts/` guarda solo transcripts activos de la sesion actual.

Formato:
- `YYYY-MM-DD_AGENTE.md`

Al ejecutar rollover (`/api/session/archive`) se archivan en `archive/sessions/.../transcripts/`.
